# 04 - Advanced Analysis

Python analytical pipeline for Meridian Business Intelligence.
All analysis is reproducible. Clone the repo, install requirements,
run the scripts, get identical outputs.
---

## Files

| File | Purpose |
|---|---|
| requirements.txt | All Python dependencies with pinned versions |
| data_generation.py | Generates clients.csv, portfolios.csv, transactions.csv |
| statistical_analysis.py | Descriptive stats, correlation matrix, AUM distribution |
| credit_risk_scoring.py | DTI and DTC ratios, four-tier risk classification |
| forecasting_model.py | 24-month AUM trend projection with confidence bounds |

---

## Why Python Over Excel

Reproducibility is the core reason. Two Python scripts in a git
diff show exactly what changed line by line. Two Excel files show
nothing meaningful. Every output here can be regenerated from
scratch by anyone who runs the scripts in sequence.

The second reason is pipeline integrity. Data flows from
data_generation.py into PostgreSQL via the seed loader, then
back out through SQL queries, then into the Python analysis
scripts. Every step is code. Every step is auditable.

---

## Setup

```bash
All chart outputs saved to `outputs/` folder.
All CSV outputs saved to `outputs/` folder.

---

## Output Files

| File | Generated By |
|---|---|
| outputs/statistical_summary.csv | statistical_analysis.py |
| outputs/correlation_matrix.png | statistical_analysis.py |
| outputs/aum_distribution.png | statistical_analysis.py |
| outputs/risk_scores.csv | credit_risk_scoring.py |
| outputs/risk_distribution.png | credit_risk_scoring.py |
| outputs/aum_forecast.png | forecasting_model.py |
| outputs/forecast_summary.csv | forecasting_model.py |
